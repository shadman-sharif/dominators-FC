rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /siteContent/main {
      allow read, write: if true;
    }
    match /adminSettings/main {
      allow read, write: if true;
    }
  }
}
